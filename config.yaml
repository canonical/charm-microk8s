# This file defines charm config options, and populates the Configure tab on Charmhub.
# If your charm does not require configuration options, delete this file entirely.
#
# See https://juju.is/docs/config for guidance.

options:
  addons:
    description: Addons to enable
    default: "dns rbac hostpath-storage ingress"
    type: string
  role:
    description: Role for this microk8s deployment
    default: ""
    type: string
  channel:
    description: MicroK8s channel to install
    default: ""
    type: string
